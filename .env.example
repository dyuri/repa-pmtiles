# Nginx Configuration
# Space-separated list of allowed referrer hostnames for PMTiles access
# Add your domain names here to restrict tile access
ALLOWED_REFERRERS=localhost 127.0.0.1 example.com yourdomain.com
